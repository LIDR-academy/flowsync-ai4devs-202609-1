1- Que Archivos que se tocaron

Con Harness: 7 archivos, solo del frontend
sin Harness: 4 archivos del frontend 1 del backend

2- Convenciones que se respetaron del proyecto

Con Harness: - Se respeto el no hacer cambios de frontend y backend en el mismo commit
              - Antes de dar por terminada una tarea de backend, corre `npm run typecheck` y `npm run lint` en `backend/`; para frontend, `npm run lint` en `frontend/`. No asumas que compila sin correrlo
Sin Harness: No hubo convenciones definidas


3- Cuantas veces tuve que intervenir

Con Harness: 1 y termino la tarea incluido el commit de los cambios. Me limite a revisar todos los pasos que realizaba y confirme todas las acciones manualmente
Sin Harness: 2 veces y  distorciono el pedido orginal

4- Que te  tocaria arreglar a mano 
Con Harness: Nado, los cambios fueron implementados
Sin Harness: Todo, no llego a realizar los cambios y propuso cambios en el backend que no fue lo pedido

Las tres lines

1- El rato se me fue en el armado del Harness, en verdad monto todo claude y yo le fui confirmando, monto 8 piezas.

2-Obviamente con el proceso con el harness salio mejor. Armo el plan mucho mas acorde con lo solicitado y solo hubo que aclarar algunas inconsistencias en el pedido. Tenia mas clara las politicas y el stack del proyecto y 
fue mucho mas concreto en el plan.
 En el caso sin harness, frente a la incosistencia entre lo pedido y lo que tenia definido en el backend (politica de contraseñas) se limito sacarlo del pedido y controlo unicamente que no sea nulo.

3- no uso el agente adversarial-reviewer para revisar los posibles riesgos de seguridad con el cambio realizado

