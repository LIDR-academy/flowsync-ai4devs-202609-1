{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // .tool_response.filePath // empty'); case \"$f\" in \"$CLAUDE_PROJECT_DIR\"/frontend/*) cd \"$CLAUDE_PROJECT_DIR/frontend\" && npx --no-install prettier --write --ignore-unknown \"$f\" ;; esac 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Formateando con Prettier (frontend)..."
          }
        ]
      }
    ]
  }
}
